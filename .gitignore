# Cargo build artifacts
/target

# Not committed: the truenas_ktls dev-dependency tracks a branch, and a
# committed lockfile would freeze it at a recorded revision until a manual
# `cargo update`. A fresh resolve follows the branch; no CI step passes
# --locked.
Cargo.lock
